---
import Layout from '../../layouts/Layout.astro';
import fs from 'node:fs';
import path from 'node:path';
import { marked } from 'marked';
const mdPath = path.resolve('..', 'designs', 'yc-demo-day', 'DESIGN.md');
const mdRaw = fs.readFileSync(mdPath, 'utf-8');
const mdHtml = await marked(mdRaw);
---

<Layout title="Y Combinator Demo Day — slide-design-md">
  <nav>
    <div class="nav-inner">
      <a href="/slide-design-md/" class="nav-back">&larr; Back to designs</a>
      <a href="https://github.com/takumaoshiro/slide-design-md" class="nav-gh" target="_blank">GitHub</a>
    </div>
  </nav>

  <!-- Header -->
  <section class="detail-header">
    <div class="container">
      <h1>Y Combinator Demo Day</h1>
      <p class="detail-tagline">Demo Day pitch. Orange accent, metric-driven, 10 slides in 2 minutes. No fluff.</p>
    </div>
  </section>

  <!-- Usage -->
  <section class="usage">
    <div class="container">
      <div class="usage-grid">
        <div class="usage-cmd">
          <div class="cmd-box">
            <code>curl -o DESIGN.md https://raw.githubusercontent.com/takumaoshiro/slide-design-md/main/designs/yc-demo-day/DESIGN.md</code>
            <button class="copy-btn" data-copy="curl -o DESIGN.md https://raw.githubusercontent.com/takumaoshiro/slide-design-md/main/designs/yc-demo-day/DESIGN.md" title="Copy">
              <svg width="16" height="16" viewBox="0 0 16 16" fill="none"><rect x="5" y="5" width="9" height="9" rx="1.5" stroke="currentColor" stroke-width="1.5"/><path d="M11 5V3.5A1.5 1.5 0 009.5 2h-6A1.5 1.5 0 002 3.5v6A1.5 1.5 0 003.5 11H5" stroke="currentColor" stroke-width="1.5"/></svg>
            </button>
          </div>
          <p class="cmd-help">Run from your project root, then ask your AI assistant to use DESIGN.md for slide generation.</p>
        </div>
        <div class="usage-info">
          <span class="info-badge">16:9</span>
          <span class="info-badge">Inter</span>
          <span class="info-badge">Pitch Deck</span>
        </div>
      </div>
    </div>
  </section>

  <!-- Preview tabs -->
  <section class="preview-section">
    <div class="container">
      <div class="tabs-bar">
        <h2>Preview</h2>
        <div class="tabs">
          <button class="tab active" data-tab="preview">Slide Preview</button>
          <button class="tab" data-tab="designmd">DESIGN.md</button>
        </div>
      </div>

      <!-- Slide Preview -->
      <div id="tab-preview" class="tab-content active">
        <div class="slides-grid">

          <!-- Cover Slide -->
          <div class="slide-label">01 — Cover</div>
          <div class="slide-frame">
            <div class="s yc-s">
              <div class="yc-cover-body">
                <div class="yc-batch">W25</div>
                <div class="yc-cover-title">Acme AI</div>
                <div class="yc-cover-sub">AI-powered infrastructure for developer tools</div>
              </div>
            </div>
          </div>

          <!-- Content Slide -->
          <div class="slide-label">02 — Content</div>
          <div class="slide-frame">
            <div class="s yc-s">
              <div class="yc-inner">
                <div class="yc-h1">Problem</div>
                <div class="yc-body-text">Developers spend 40% of their time on infrastructure that has nothing to do with their product.</div>
                <div class="yc-metric-highlight">
                  <div class="yc-big-num">40%</div>
                  <div class="yc-big-label">of engineering time wasted</div>
                </div>
              </div>
            </div>
          </div>

          <!-- Data / Table Slide -->
          <div class="slide-label">03 — Data / Table</div>
          <div class="slide-frame">
            <div class="s yc-s">
              <div class="yc-inner">
                <div class="yc-h1">Unit Economics</div>
                <table class="yc-table">
                  <thead>
                    <tr>
                      <th>Metric</th><th>Value</th>
                    </tr>
                  </thead>
                  <tbody>
                    <tr><td>ACV</td><td>$48,000</td></tr>
                    <tr><td>CAC</td><td>$3,200</td></tr>
                    <tr><td>LTV/CAC</td><td style="color:#F26522;font-weight:700;">15x</td></tr>
                    <tr><td>Gross Margin</td><td>89%</td></tr>
                    <tr><td>Net Revenue Retention</td><td style="color:#16A34A;font-weight:700;">142%</td></tr>
                  </tbody>
                </table>
              </div>
            </div>
          </div>

          <!-- KPI / Traction Slide -->
          <div class="slide-label">04 — KPI / Traction</div>
          <div class="slide-frame">
            <div class="s yc-s">
              <div class="yc-inner yc-center">
                <div class="yc-h1">Traction</div>
                <div class="yc-hero-metric">
                  <div class="yc-hero-num">$1.2M</div>
                  <div class="yc-hero-label">ARR</div>
                </div>
                <div class="yc-growth-row">
                  <span class="yc-growth-badge" style="color:#16A34A;">4x YoY</span>
                  <span class="yc-growth-badge" style="color:#F26522;">32% MoM</span>
                </div>
              </div>
            </div>
          </div>

          <!-- Color Palette -->
          <div class="slide-label">05 — Color Palette</div>
          <div class="slide-frame">
            <div class="s yc-s">
              <div class="yc-inner">
                <div class="yc-h1">Color Palette</div>
                <div class="yc-palette">
                  <div class="yc-swatch"><div class="swatch-block" style="background:#FFFFFF; border:1px solid #E5E5E5;"></div><div class="swatch-name">White<br/>#FFFFFF</div></div>
                  <div class="yc-swatch"><div class="swatch-block" style="background:#111111;"></div><div class="swatch-name">Black<br/>#111111</div></div>
                  <div class="yc-swatch"><div class="swatch-block" style="background:#333333;"></div><div class="swatch-name">Body<br/>#333333</div></div>
                  <div class="yc-swatch"><div class="swatch-block" style="background:#888888;"></div><div class="swatch-name">Muted<br/>#888888</div></div>
                  <div class="yc-swatch"><div class="swatch-block" style="background:#F26522;"></div><div class="swatch-name">YC Orange<br/>#F26522</div></div>
                  <div class="yc-swatch"><div class="swatch-block" style="background:#FFF3EB;"></div><div class="swatch-name">Orange Light<br/>#FFF3EB</div></div>
                  <div class="yc-swatch"><div class="swatch-block" style="background:#16A34A;"></div><div class="swatch-name">Green<br/>#16A34A</div></div>
                  <div class="yc-swatch"><div class="swatch-block" style="background:#DC2626;"></div><div class="swatch-name">Red<br/>#DC2626</div></div>
                </div>
              </div>
            </div>
          </div>

          <!-- Typography -->
          <div class="slide-label">06 — Typography</div>
          <div class="slide-frame">
            <div class="s yc-s">
              <div class="yc-inner">
                <div class="yc-h1">Typography Scale</div>
                <div class="yc-type-samples">
                  <div class="yc-type-row">
                    <span class="yc-type-demo" style="font-family:'Inter',sans-serif;font-size:20px;font-weight:700;color:#111111;">Cover Title — 48pt Inter Bold</span>
                    <span class="yc-type-meta">Bold / #111111</span>
                  </div>
                  <div class="yc-type-row">
                    <span class="yc-type-demo" style="font-family:'Inter',sans-serif;font-size:14px;font-weight:700;color:#111111;">Slide Title — 32pt Inter Bold</span>
                    <span class="yc-type-meta">Bold / #111111</span>
                  </div>
                  <div class="yc-type-row">
                    <span class="yc-type-demo" style="font-family:'Inter',sans-serif;font-size:28px;font-weight:700;color:#F26522;">$1.2M — 80pt+ Key Metric</span>
                    <span class="yc-type-meta">Bold / Orange</span>
                  </div>
                  <div class="yc-type-row">
                    <span class="yc-type-demo" style="font-family:'Inter',sans-serif;font-size:9px;color:#333333;">Body text — 16pt Inter Regular. One or two sentences max.</span>
                    <span class="yc-type-meta">Regular / #333333</span>
                  </div>
                  <div class="yc-type-row">
                    <span class="yc-type-demo" style="font-family:'Inter',sans-serif;font-size:7px;color:#888888;">Label / Caption — 12pt Inter Regular</span>
                    <span class="yc-type-meta">Regular / #888888</span>
                  </div>
                </div>
              </div>
            </div>
          </div>

        </div>
      </div>

      <!-- DESIGN.md tab -->
      <div id="tab-designmd" class="tab-content">
        <div class="md-frame">
          <div class="md-toolbar">
            <button class="copy-md-btn" id="copy-md">
              <svg width="16" height="16" viewBox="0 0 16 16" fill="none"><rect x="5" y="5" width="9" height="9" rx="1.5" stroke="currentColor" stroke-width="1.5"/><path d="M11 5V3.5A1.5 1.5 0 009.5 2h-6A1.5 1.5 0 002 3.5v6A1.5 1.5 0 003.5 11H5" stroke="currentColor" stroke-width="1.5"/></svg>
              <span>Copy DESIGN.md</span>
            </button>
          </div>
          <div class="md-body" set:html={mdHtml} />
        </div>
        <textarea id="md-raw" style="display:none;">{mdRaw}</textarea>
      </div>

    </div>
  </section>

  <footer>
    <div class="container">
      <p>slide-design-md &mdash; Built for AI agents that create presentations.</p>
    </div>
  </footer>
</Layout>

<style>
  nav { position: sticky; top: 0; z-index: 100; background: rgba(0,0,0,0.8); backdrop-filter: saturate(180%) blur(20px); -webkit-backdrop-filter: saturate(180%) blur(20px); height: 48px; }
  .nav-inner { max-width: 980px; margin: 0 auto; padding: 0 1.5rem; height: 100%; display: flex; align-items: center; justify-content: space-between; }
  .nav-back { font-size: 14px; color: #2997ff; letter-spacing: -0.224px; }
  .nav-back:hover { text-decoration: underline; }
  .nav-gh { font-size: 12px; color: #f5f5f7; opacity: 0.8; }
  .container { max-width: 980px; margin: 0 auto; padding: 0 1.5rem; }

  .detail-header { background: var(--black); padding: 3rem 0 2.5rem; }
  .detail-header h1 { font-family: -apple-system, BlinkMacSystemFont, 'SF Pro Display', sans-serif; font-size: 40px; font-weight: 600; color: var(--white); line-height: 1.1; letter-spacing: -0.5px; margin-bottom: 0.5rem; }
  .detail-tagline { font-size: 17px; color: rgba(255,255,255,0.55); letter-spacing: -0.374px; }

  .usage { background: var(--black); padding: 0 0 2.5rem; border-bottom: 1px solid rgba(255,255,255,0.1); }
  .usage-grid { display: flex; gap: 2rem; align-items: flex-start; }
  .usage-cmd { flex: 1; }
  .cmd-box { background: #0a0a0a; border: 1px solid rgba(255,255,255,0.1); border-radius: 8px; padding: 12px 16px; display: flex; align-items: center; gap: 12px; position: relative; }
  .cmd-box code { font-family: 'SF Mono','Consolas',monospace; font-size: 13px; color: #e0e0e0; word-break: break-all; flex: 1; }
  .copy-btn { background: none; border: none; color: rgba(255,255,255,0.4); cursor: pointer; padding: 4px; transition: color 0.2s; flex-shrink: 0; }
  .copy-btn:hover { color: var(--white); }
  .cmd-help { font-size: 12px; color: rgba(255,255,255,0.35); margin-top: 0.5rem; letter-spacing: -0.12px; }
  .usage-info { display: flex; gap: 0.5rem; flex-wrap: wrap; padding-top: 4px; }
  .info-badge { font-size: 12px; color: rgba(255,255,255,0.5); border: 1px solid rgba(255,255,255,0.15); border-radius: 4px; padding: 4px 10px; letter-spacing: -0.12px; }

  .preview-section { background: var(--light-bg); padding: 3rem 0 4rem; }
  .tabs-bar { display: flex; justify-content: space-between; align-items: center; margin-bottom: 2rem; }
  .tabs-bar h2 { font-family: -apple-system, BlinkMacSystemFont, 'SF Pro Display', sans-serif; font-size: 28px; font-weight: 600; color: var(--near-black); }
  .tabs { display: flex; border: 1px solid rgba(0,0,0,0.15); border-radius: 8px; overflow: hidden; }
  .tab { padding: 6px 18px; font-size: 14px; font-family: inherit; letter-spacing: -0.224px; border: none; background: transparent; color: var(--text-80); cursor: pointer; transition: all 0.2s; }
  .tab.active { background: var(--near-black); color: var(--white); }
  .tab-content { display: none; }
  .tab-content.active { display: block; }

  /* Slides grid */
  .slides-grid { display: flex; flex-direction: column; gap: 1rem; }
  .slide-label { font-size: 12px; font-weight: 600; color: var(--text-48); letter-spacing: 0.5px; text-transform: uppercase; margin-top: 1rem; }
  .slide-frame { border: 1px solid rgba(0,0,0,0.08); border-radius: 8px; overflow: hidden; box-shadow: 0 1px 4px rgba(0,0,0,0.06); }

  /* Base slide */
  .s { width: 100%; aspect-ratio: 16/9; position: relative; overflow: hidden; }
  .yc-s { background: #FFFFFF; font-family: 'Inter', -apple-system, BlinkMacSystemFont, sans-serif; }

  /* Cover */
  .yc-cover-body { display: flex; flex-direction: column; align-items: center; justify-content: center; height: 100%; padding: 8%; gap: 2%; }
  .yc-batch { font-size: clamp(8px, 1.2vw, 14px); font-weight: 700; color: #F26522; letter-spacing: 1px; }
  .yc-cover-title { font-size: clamp(18px, 4vw, 48px); font-weight: 700; color: #111111; text-align: center; }
  .yc-cover-sub { font-size: clamp(7px, 1.2vw, 18px); color: #888888; text-align: center; }

  /* Inner layout */
  .yc-inner { padding: 6% 8% 5%; height: 100%; display: flex; flex-direction: column; }
  .yc-inner.yc-center { align-items: center; justify-content: center; }
  .yc-h1 { font-size: clamp(12px, 2.6vw, 32px); font-weight: 700; color: #111111; margin-bottom: 3%; }
  .yc-body-text { font-size: clamp(7px, 1.2vw, 16px); color: #333333; line-height: 1.5; max-width: 80%; }

  /* Metric highlight */
  .yc-metric-highlight { margin-top: auto; margin-bottom: 6%; }
  .yc-big-num { font-size: clamp(20px, 5vw, 60px); font-weight: 700; color: #F26522; }
  .yc-big-label { font-size: clamp(6px, 1vw, 14px); color: #888888; margin-top: 1%; }

  /* Hero metric (traction slide) */
  .yc-hero-metric { margin: 2% 0; }
  .yc-hero-num { font-size: clamp(28px, 7vw, 80px); font-weight: 700; color: #F26522; text-align: center; }
  .yc-hero-label { font-size: clamp(8px, 1.4vw, 18px); color: #888888; text-align: center; margin-top: 1%; }
  .yc-growth-row { display: flex; gap: 5%; margin-top: 3%; }
  .yc-growth-badge { font-size: clamp(8px, 1.4vw, 18px); font-weight: 700; }

  /* Table */
  .yc-table { width: 100%; border-collapse: collapse; margin-top: 2%; flex: 1; max-width: 70%; }
  .yc-table th { font-size: clamp(5px, 0.9vw, 14px); font-weight: 700; color: #111111; padding: 2% 3%; text-align: left; border-bottom: 2px solid #111111; }
  .yc-table td { font-size: clamp(5px, 0.9vw, 14px); color: #333333; padding: 2% 3%; border-bottom: 1px solid #E5E5E5; }

  /* Palette */
  .yc-palette { display: flex; flex-wrap: wrap; gap: 2.5%; margin-top: 3%; }
  .yc-swatch { display: flex; flex-direction: column; align-items: center; width: 9%; }
  .swatch-block { width: 100%; aspect-ratio: 1; border-radius: 4px; }
  .swatch-name { font-size: clamp(4px, 0.6vw, 7px); color: #333333; text-align: center; margin-top: 4px; line-height: 1.3; }

  /* Typography */
  .yc-type-samples { display: flex; flex-direction: column; gap: 3%; margin-top: 2%; flex: 1; }
  .yc-type-row { display: flex; justify-content: space-between; align-items: baseline; padding: 1.5% 0; border-bottom: 1px solid #F5F5F5; }
  .yc-type-demo { flex: 1; }
  .yc-type-meta { font-size: clamp(4px, 0.6vw, 7px); color: #888888; flex-shrink: 0; margin-left: 4%; }

  /* DESIGN.md frame */
  .md-frame { border: 1px solid rgba(0,0,0,0.08); border-radius: 8px; overflow: hidden; background: #fff; padding: 0 2.5rem 2rem; }
  .md-toolbar { display: flex; justify-content: flex-end; padding: 1rem 0; position: sticky; top: 48px; background: #fff; z-index: 10; border-bottom: 1px solid #e5e7f0; }
  .copy-md-btn { display: inline-flex; align-items: center; gap: 6px; padding: 6px 14px; border-radius: 6px; border: 1px solid rgba(0,0,0,0.15); background: #fff; color: var(--near-black); font-size: 13px; font-family: inherit; letter-spacing: -0.12px; cursor: pointer; transition: all 0.2s; }
  .copy-md-btn:hover { background: var(--light-bg); border-color: rgba(0,0,0,0.3); }
  .copy-md-btn.copied { background: #e8f5e9; border-color: #2e7d32; color: #2e7d32; }
  .md-body { font-family: -apple-system, BlinkMacSystemFont, 'SF Pro Text', sans-serif; font-size: 15px; color: #1d1d1f; line-height: 1.6; }
  .md-body h1 { font-size: 28px; font-weight: 600; margin: 2rem 0 1rem; color: #1d1d1f; border-bottom: 1px solid #e5e7f0; padding-bottom: 0.5rem; }
  .md-body h2 { font-size: 22px; font-weight: 600; margin: 2rem 0 0.75rem; color: #1d1d1f; }
  .md-body h3 { font-size: 17px; font-weight: 600; margin: 1.5rem 0 0.5rem; color: #1d1d1f; }
  .md-body p { margin: 0.75rem 0; }
  .md-body ul, .md-body ol { margin: 0.5rem 0; padding-left: 1.5rem; }
  .md-body li { margin: 0.25rem 0; }
  .md-body code { font-family: 'SF Mono','Consolas',monospace; font-size: 13px; background: #f5f5f7; padding: 2px 6px; border-radius: 4px; }
  .md-body pre { background: #1d1d1f; color: #e0e0e0; padding: 1rem 1.25rem; border-radius: 8px; overflow-x: auto; margin: 1rem 0; }
  .md-body pre code { background: none; padding: 0; color: inherit; font-size: 13px; }
  .md-body table { width: 100%; border-collapse: collapse; margin: 1rem 0; font-size: 14px; }
  .md-body th { text-align: left; font-weight: 600; padding: 8px 12px; border-bottom: 2px solid #e5e7f0; }
  .md-body td { padding: 8px 12px; border-bottom: 1px solid #e5e7f0; }
  .md-body hr { border: none; border-top: 1px solid #e5e7f0; margin: 2rem 0; }
  .md-body strong { font-weight: 600; }
  .md-body blockquote { border-left: 3px solid #0071e3; margin: 1rem 0; padding: 0.5rem 1rem; background: #f5f5f7; border-radius: 0 4px 4px 0; }

  footer { background: var(--light-bg); padding: 2rem 0; text-align: center; border-top: 1px solid rgba(0,0,0,0.06); }
  footer p { font-size: 12px; color: var(--text-48); }

  @media (max-width: 834px) {
    .usage-grid { flex-direction: column; }
    .detail-header h1 { font-size: 32px; }
  }
</style>

<script>
  // Tab switching
  document.querySelectorAll('.tab').forEach(btn => {
    btn.addEventListener('click', () => {
      document.querySelectorAll('.tab').forEach(b => b.classList.remove('active'));
      document.querySelectorAll('.tab-content').forEach(c => c.classList.remove('active'));
      btn.classList.add('active');
      const target = (btn as HTMLElement).dataset.tab;
      document.getElementById('tab-' + target)?.classList.add('active');
    });
  });
  // Copy command button
  document.querySelectorAll('.copy-btn').forEach(btn => {
    btn.addEventListener('click', () => {
      const text = (btn as HTMLElement).dataset.copy || '';
      navigator.clipboard.writeText(text);
      btn.innerHTML = '<span style="font-size:12px;color:#2E7D32;">Copied!</span>';
      setTimeout(() => {
        btn.innerHTML = '<svg width="16" height="16" viewBox="0 0 16 16" fill="none"><rect x="5" y="5" width="9" height="9" rx="1.5" stroke="currentColor" stroke-width="1.5"/><path d="M11 5V3.5A1.5 1.5 0 009.5 2h-6A1.5 1.5 0 002 3.5v6A1.5 1.5 0 003.5 11H5" stroke="currentColor" stroke-width="1.5"/></svg>';
      }, 1500);
    });
  });
  // Copy DESIGN.md
  document.getElementById('copy-md')?.addEventListener('click', () => {
    const raw = (document.getElementById('md-raw') as HTMLTextAreaElement)?.value || '';
    navigator.clipboard.writeText(raw);
    const btn = document.getElementById('copy-md')!;
    btn.classList.add('copied');
    btn.querySelector('span')!.textContent = 'Copied!';
    setTimeout(() => {
      btn.classList.remove('copied');
      btn.querySelector('span')!.textContent = 'Copy DESIGN.md';
    }, 2000);
  });
</script>
